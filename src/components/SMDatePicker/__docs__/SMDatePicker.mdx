import { Canvas, Meta } from '@storybook/blocks';
import Example from './Example.tsx';
import * as SMDatePicker from './SMDatePicker.stories.tsx';

<Meta of={SMDatePicker} title="SMDatePicker" />

# SMDatePicker

SMDatePicker component with different props.

#### Example

<Canvas of={SMDatePicker.DatePicker} />

## Usage

```ts
import {SMDatePicker} from "sld-ui";

const Example = () => {
  return (
     <SMDatePicker />
  );
};

export default Example;
```

#### Arguments
