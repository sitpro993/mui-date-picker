import { Canvas, Meta } from '@storybook/blocks';
import Example from './Example.tsx';
import * as SMRangePicker from './SMRangePicker.stories.tsx';

<Meta of={SMRangePicker} title="SMRangePicker" />

# SMRangePicker

SMRangePicker component with different props.

#### Example

<Canvas of={SMRangePicker.RangePicker} />

## Usage

```ts
import {SMRangePicker} from "sld-ui";

const Example = () => {
  return (
     <SMRangePicker />
  );
};

export default Example;
```

#### Arguments
