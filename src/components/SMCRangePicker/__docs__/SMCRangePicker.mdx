import { Canvas, Meta } from '@storybook/blocks';
import Example from './Example.tsx';
import * as SMCRangePicker from './SMCRangePicker.stories.tsx';

<Meta of={SMCRangePicker} title="SMCRangePicker" />

# SMCRangePicker

SMCRangePicker component with different props.

#### Example

<Canvas of={SMCRangePicker.RangePicker} />

## Usage

```ts
import {SMCRangePicker} from "sld-ui";

const Example = () => {
  return (
     <SMCRangPicker />
  );
};

export default Example;
```

#### Arguments
